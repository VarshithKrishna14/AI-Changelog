// This project currently runs Corsair with direct plugin credentials (no DB-backed key storage).
// Kept as a placeholder module to avoid stale imports in local editor history.
export async function migrate() {
  // no-op
}
